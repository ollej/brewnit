<% if @error.nil? %>
  $("#hops").find("tr[data-hop-id=<%= @hop.id %>]")
    .effect("highlight", {}, 500, function() {
      this.remove();
    });
<% else %>
  $('body').trigger('flasher:show', {
    message: '<%= j(@error) %>',
    level: 'error'
  })
<% end %>
