<% if @error.nil? %>
  $("#mash_steps").find("tr[data-mash_step-id=<%= @mash_step.id %>]")
    .effect("highlight", {}, 500, function() {
      this.remove();
    });
<% else %>
  $('body').trigger('flasher:show', {
    message: '<%= j(@error) %>',
    level: 'error'
  })
<% end %>
