<% if @error.nil? %>
  $("#yeasts").find("tr[data-yeast-id=<%= @yeast.id %>]")
    .effect("fade", {}, 500, function() {
      this.remove();
    });
<% else %>
  $('body').trigger('flasher:show', {
    message: '<%= j(@error) %>',
    level: 'error'
  })
<% end %>
