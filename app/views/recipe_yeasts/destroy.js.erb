<% if @error.nil? %>
  $("#yeasts").find("li[data-yeast-id=<%= @yeast.id %>]")
    .effect("highlight", {}, 500, function() {
      this.remove();
    });
<% else %>
  $('body').trigger('flasher:show', {
    message: '<%= j(@error) %>',
    level: 'error'
  })
<% end %>
