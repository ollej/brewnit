      <div class="recipe-content-header pure-g">
        <div class="pure-u-1-2">
          <h1 class="recipe-content-title"><%= @recipe.name %></h1>
          <p class="recipe-content-subtitle">
            <%= @recipe.description %>
          </p>

        </div>

        <div class="recipe-content-controls pure-u-1-2">
          <%= link_to I18n.t(:'recipes.download'), recipe_path(@recipe, format: :xml), method: :get, class: 'pure-button secondary-button' %>
          <% if user_signed_in? %>
            <%#= link_to I18n.t(:'recipes.clone'), clone_recipe_path(@recipe), class: 'pure-button secondary-button' %>
            <% if current_user.can_modify?(@recipe) %>
            <%= link_to I18n.t(:'recipes.edit'), edit_recipe_path(@recipe), class: 'pure-button secondary-button' %>
            <%= link_to I18n.t(:'recipes.destroy.link'), @recipe, method: :delete, data: { confirm: I18n.t(:'common.are_you_sure') }, class: 'pure-button secondary-button' %>
            <% end %>
          <% end %>
        </div>

        <div class="recipe-badge-list pure-u">
        <%= visibility_badge(@recipe) %>
        </div>

      </div>

      <div class="recipe-content-body">
        <div class="tabs">
          <input type="radio" name="tabs" id="tab1" checked>
          <label for="tab1" class="tab-trigger">
            <i class="fa fa-beer"></i><span><%= I18n.t(:'recipes.recipe_details') %></span>
          </label>
          <input type="radio" name="tabs" id="tab2">
          <label for="tab2" class="tab-trigger">
            <i class="fa fa-pie-chart"></i><span><%= I18n.t(:'recipes.recipe_analytics') %></span>
          </label>
          <div id="tab-content1" class="tab-content">
<%= render 'shared/recipe_details' %>
          </div>
          <div id="tab-content2" class="tab-content">
<%= render 'shared/malt_pie' %>
          </div>
        </div>
      </div>
